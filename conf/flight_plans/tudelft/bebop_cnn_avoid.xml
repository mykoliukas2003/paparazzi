<?xml version="1.0"?>
<!DOCTYPE flight_plan SYSTEM "../flight_plan.dtd">
<flight_plan alt="1.0" ground_alt="0" lat0="51.990634" lon0="4.376789" max_dist_from_home="60" name="Bebop CNN nav trajectories TU Delft Cyberzoo" security_height="0.4">

  <!--=== Header ===-->
  <header>
    #include "modules/datalink/datalink.h"
    #include "modules/energy/electrical.h"
    #include "modules/radio_control/radio_control.h"
    #include "modules/ahrs/ahrs.h"
    #include "modules/cnn_avoid/cnn_avoid.h"
    #define NPS_GAZEBO_WORLD "cyberzoo_orange_poles.world"
  </header>

  <!--=== Waypoints ===-->
  <waypoints>
    <!-- Standard navigation waypoints -->
    <waypoint lat="51.990631" lon="4.376796" name="HOME"/>
    <waypoint name="CLIMB"      x="0.0"  y="0.0"/>
    <waypoint name="STDBY"      x="0.0"  y="0.0"/>
    <waypoint name="TD"         x="0.0"  y="-1.5" height="0.5"/>

    <!-- Avoider dynamic waypoints (moved by C code each cycle) -->
    <waypoint name="GOAL"       x="0.0"  y="0.5"/>
    <waypoint name="TRAJECTORY" x="0.0"  y="0.5"/>
    <waypoint name="RETREAT"    x="0.0"  y="-0.5"/>

    <!-- Trajectory anchor — centre of the arena -->
    <waypoint name="CENTER"     x="0.0"  y="0.0"/>

    <!-- CyberZoo outer boundary corners -->
    <waypoint lat="51.9905834" lon="4.3767710" name="_CZ1"/>
    <waypoint lat="51.9906440" lon="4.3767060" name="_CZ2"/>
    <waypoint lat="51.9906860" lon="4.3768080" name="_CZ3"/>
    <waypoint lat="51.9906238" lon="4.3768729" name="_CZ4"/>

    <!-- Obstacle zone inner boundary corners -->
    <waypoint lat="51.9906006" lon="4.3767764" name="_OZ1"/>
    <waypoint lat="51.9906405" lon="4.3767316" name="_OZ2"/>
    <waypoint lat="51.9906687" lon="4.3768025" name="_OZ3"/>
    <waypoint lat="51.9906273" lon="4.3768438" name="_OZ4"/>
  </waypoints>

  <!--=== Sectors ===-->
  <sectors>
    <sector color="red" name="CyberZoo">
      <corner name="_CZ1"/>
      <corner name="_CZ2"/>
      <corner name="_CZ3"/>
      <corner name="_CZ4"/>
    </sector>
    <sector color="#FF9922" name="ObstacleZone">
      <corner name="_OZ1"/>
      <corner name="_OZ2"/>
      <corner name="_OZ3"/>
      <corner name="_OZ4"/>
    </sector>
  </sectors>

  <!--=== Safety Exceptions ===-->
  <exceptions>
    <!-- Datalink lost -->
    <exception cond="((datalink_time @GT 5) @AND
      !(IndexOfBlock('Holding point') @GT nav_block) @AND
      !(nav_block >= IndexOfBlock('land here')) @AND
      (autopilot_in_flight() == true) )" deroute="land here"/>
    <!-- Outside CyberZoo XY -->
    <exception cond="(!InsideCyberZoo(GetPosX(), GetPosY()) @AND
      !(IndexOfBlock('Holding point') @GT nav_block) @AND
      !(nav_block >= IndexOfBlock('land here')) @AND
      (autopilot_in_flight() == true) )" deroute="land here"/>
    <!-- Altitude ceiling 3.5m -->
    <exception cond="((GetPosAlt() @GT 3.5) @AND
      !(IndexOfBlock('Holding point') @GT nav_block) @AND
      !(nav_block >= IndexOfBlock('land here')) @AND
      (autopilot_in_flight() == true) )" deroute="land here"/>
    <!-- Altitude ceiling 4.5m (kill) -->
    <exception cond="((GetPosAlt() @GT 4.5) @AND
      !(IndexOfBlock('Holding point') @GT nav_block) @AND
      (autopilot_in_flight() == true) )" deroute="Landed"/>
    <!-- Battery low -->
    <exception cond="(electrical.bat_low @AND
      !(IndexOfBlock('Holding point') @GT nav_block) @AND
      !(nav_block >= IndexOfBlock('land here')) @AND
      (autopilot_in_flight() == true) )" deroute="land here"/>
    <!-- Battery critical -->
    <exception cond="(electrical.bat_critical @AND
      !(IndexOfBlock('Holding point') @GT nav_block) @AND
      !(nav_block >= IndexOfBlock('land here')) @AND
      (autopilot_in_flight() == true) )" deroute="land here"/>
  </exceptions>

  <!--=== Mission Blocks ===-->
  <blocks>

    <!-- ===== Startup sequence ===== -->

    <block name="Wait GPS">
      <call_once fun="NavKillThrottle()"/>
      <while cond="!GpsFixValid()"/>
    </block>

    <block name="Geo init">
      <while cond="LessThan(NavBlockTime(), 2)"/>
      <call_once fun="NavSetAltitudeReferenceHere()"/>
    </block>

    <block name="Holding point">
      <call_once fun="NavKillThrottle()"/>
      <attitude pitch="0" roll="0" throttle="0" until="FALSE" vmode="throttle"/>
    </block>

    <block key="r" name="Start Engine">
      <call_once fun="NavResurrect()"/>
      <attitude pitch="0" roll="0" throttle="0" until="FALSE" vmode="throttle"/>
      <call_once fun="NavResurrect()"/>
    </block>

    <block key="t" name="Takeoff" strip_button="Takeoff" strip_icon="takeoff.png">
      <call_once fun="NavSetWaypointHere(WP_TD)"/>
      <exception cond="GetPosAlt() @GT 0.8" deroute="Standby"/>
      <call_once fun="NavSetWaypointHere(WP_CLIMB)"/>
      <stay climb="nav.climb_vspeed" vmode="climb" wp="CLIMB"/>
    </block>

    <block key="s" name="Standby" strip_button="Standby" strip_icon="home.png">
      <call_once fun="NavSetWaypointHere(WP_STDBY)"/>
      <stay wp="STDBY"/>
    </block>

    <!-- ===== Trajectory selection blocks ===== -->
     <block key="0" name="START free roam" strip_button="Free Roam" strip_icon="lookfore.png">
      <call_once fun="cnn_avoid_set_trajectory(0)"/>
      <call_once fun="NavSetWaypointHere(WP_GOAL)"/>
      <call_once fun="NavSetWaypointHere(WP_TRAJECTORY)"/>
      <call_once fun="NavSetWaypointHere(WP_RETREAT)"/>
      <stay wp="GOAL"/>
    </block>

    <block key="1" name="START circle" strip_button="Circle" strip_icon="circle.png">
      <call_once fun="cnn_avoid_set_trajectory(1)"/>
      <call_once fun="NavSetWaypointHere(WP_GOAL)"/>
      <call_once fun="NavSetWaypointHere(WP_TRAJECTORY)"/>
      <call_once fun="NavSetWaypointHere(WP_RETREAT)"/>
      <stay wp="GOAL"/>
    </block>

    <block key="2" name="START figure eight" strip_button="Figure-8" strip_icon="eight.png">
      <call_once fun="cnn_avoid_set_trajectory(2)"/>
      <call_once fun="NavSetWaypointHere(WP_GOAL)"/>
      <call_once fun="NavSetWaypointHere(WP_TRAJECTORY)"/>
      <call_once fun="NavSetWaypointHere(WP_RETREAT)"/>
      <stay wp="GOAL"/>
    </block>

    <block key="3" name="START lawnmower" strip_button="Lawnmower" strip_icon="survey.png">
      <call_once fun="cnn_avoid_set_trajectory(3)"/>
      <call_once fun="NavSetWaypointHere(WP_GOAL)"/>
      <call_once fun="NavSetWaypointHere(WP_TRAJECTORY)"/>
      <call_once fun="NavSetWaypointHere(WP_RETREAT)"/>
      <stay wp="GOAL"/>
    </block>

    <!-- ===== Stop / Retreat ===== -->

    <block name="STOP">
      <call_once fun="NavSetWaypointHere(WP_STDBY)"/>
      <call_once fun="NavSetWaypointHere(WP_GOAL)"/>
      <call_once fun="NavSetWaypointHere(WP_TRAJECTORY)"/>
      <call_once fun="NavSetWaypointHere(WP_RETREAT)"/>
      <stay wp="STDBY"/>
    </block>

    <block name="RETREAT">
      <exception cond="block_time @GT 5.0" deroute="STOP"/>
      <call_once fun="NavSetWaypointHere(WP_RETREAT)"/>
      <stay wp="RETREAT"/>
    </block>

    <!-- ===== Emergency / Landing sequence ===== -->

    <block key="e" name="Emergency Landing">
      <exception cond="!nav_is_in_flight()" deroute="KILL"/>
      <exception cond="0.10 @GT GetPosAlt()" deroute="KILL"/>
      <call_once fun="NavSetWaypointHere(WP_TD)"/>
      <call_once fun="NavStartDetectGround()"/>
      <stay climb="-1.25" vmode="climb" wp="TD"/>
    </block>

    <block key="l" name="land here" strip_button="land here" strip_icon="land-right.png">
      <call_once fun="NavSetWaypointHere(WP_TD)"/>
      <go wp="TD"/>
      <deroute block="Flare"/>
    </block>

    <block name="Land">
      <exception cond="0.55 @GEQ GetPosAlt()" deroute="Flare"/>
      <exception cond="block_time @GT 5" deroute="Flare"/>
      <go wp="TD"/>
      <deroute block="Flare"/>
    </block>

    <block name="Flare">
      <exception cond="NavDetectGround()" deroute="Holding point"/>
      <exception cond="!nav_is_in_flight()" deroute="Landed"/>
      <exception cond="0.10 @GT GetPosAlt()" deroute="Landed"/>
      <call_once fun="NavStartDetectGround()"/>
      <stay climb="nav.descend_vspeed" vmode="climb" wp="TD"/>
    </block>

    <block name="Landed">
      <call_once fun="NavKillThrottle()"/>
      <attitude pitch="0" roll="0" throttle="0" until="FALSE" vmode="throttle"/>
    </block>

    <block name="KILL">
      <call_once fun="autopilot_set_mode(AP_MODE_KILL)"/>
      <call_once fun="NavKillThrottle()"/>
      <attitude pitch="0" roll="0" throttle="0" until="FALSE" vmode="throttle"/>
    </block>

  </blocks>
</flight_plan>